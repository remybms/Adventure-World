openapi: 3.0.3

info:
  title: API Gestion des Aventuriers
  description: API REST sécurisée pour la gestion des aventuriers et des compétences
  version: 2.0.0

servers:
  - url: http://localhost:8080
  - url: https://aventuriers.digicrafters.fr

tags:
  - name: Auth
  - name: Aventuriers
  - name: Competences

security:
  - bearerAuth: []

paths:

  ########################
  # AUTH (PUBLIC)
  ########################

  /auth/register:
    post:
      tags: [Auth]
      summary: Inscription utilisateur (PUBLIC)
      operationId: inscription
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AuthRequest'
      responses:
        '201':
          description: Utilisateur créé

  /auth/login:
    post:
      tags: [Auth]
      summary: Connexion utilisateur (PUBLIC)
      operationId: connexion
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AuthRequest'
      responses:
        '200':
          description: Token JWT
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'

  /auth/logout:
    post:
      tags: [Auth]
      summary: Déconnexion (AUTHENTIFIÉ)
      operationId: deconnexion
      security:
        - bearerAuth: []
      responses:
        '204':
          description: Déconnecté

  ########################
  # AVENTURIERS
  ########################

  /api/v1/aventuriers:
    get:
      tags: [Aventuriers]
      summary: Lister tous les aventuriers
      operationId: listerAventuriers
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AventurierDto'
        '500':
          $ref: '#/components/responses/ErreurServeur'

    post:
      tags: [Aventuriers]
      summary: Créer un aventurier
      operationId: creerAventurier
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AventurierPayload'
      responses:
        '201':
          description: Créé
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AventurierDto'
        '400':
          $ref: '#/components/responses/RequeteInvalide'
        '500':
          $ref: '#/components/responses/ErreurServeur'

  /api/v1/aventuriers/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid

    get:
      tags: [Aventuriers]
      summary: Consulter un aventurier
      operationId: obtenirAventurier
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AventurierDto'
        '404':
          $ref: '#/components/responses/NonTrouve'
        '500':
          $ref: '#/components/responses/ErreurServeur'

    put:
      tags: [Aventuriers]
      summary: Modifier un aventurier
      operationId: modifierAventurier
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AventurierPayload'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AventurierDto'
        '400':
          $ref: '#/components/responses/RequeteInvalide'
        '404':
          $ref: '#/components/responses/NonTrouve'
        '500':
          $ref: '#/components/responses/ErreurServeur'

    delete:
      tags: [Aventuriers]
      summary: Supprimer un aventurier
      operationId: supprimerAventurier
      responses:
        '204':
          description: Supprimé
        '404':
          $ref: '#/components/responses/NonTrouve'
        '500':
          $ref: '#/components/responses/ErreurServeur'

  ########################
  # COMPETENCES
  ########################

  /api/v1/competences:
    get:
      tags: [Competences]
      summary: Lister les compétences
      operationId: listerCompetences
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Liste des compétences
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Competence'

    post:
      tags: [Competences]
      summary: Créer une compétence
      operationId: creerCompetence
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Competence'
      security:
        - bearerAuth: []
      responses:
        '201':
          description: Créée
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Competence'

  /api/v1/competences/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid

    get:
      tags: [Competences]
      summary: Détail compétence
      operationId: obtenirCompetence
      security:
        - bearerAuth: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Competence'
        '404':
          description: Ressource introuvable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

    put:
      tags: [Competences]
      summary: Modifier compétence
      operationId: modifierCompetence
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Competence'
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Modifiée
        '404':
          description: Ressource introuvable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '409':
          description: Modification rejetée
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AventurierProblemDetail'

    delete:
      tags: [Competences]
      summary: Supprimer compétence
      operationId: supprimerCompetence
      security:
        - bearerAuth: []
      responses:
        '204':
          description: Supprimée
        '404':
          description: Ressource introuvable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  ########################
  # ASSOCIATION
  ########################

  /api/v1/aventuriers/{id}/competences:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags: [Competences]
      summary: Compétences d’un aventurier
      operationId: listerCompetencesAventurier
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Liste
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Competence'
        '404':
          description: Ressource introuvable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /api/v1/aventuriers/{id}/competences/{cId}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: cId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags: [Competences]
      summary: Ajouter compétence
      operationId: ajouterCompetenceAventurier
      security:
        - bearerAuth: []
      responses:
        '204':
          description: Ajoutée
        '404':
          description: Ressource introuvable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '422':
          description: Prérequis manquant(s)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AventurierProblemDetail'

    delete:
      tags: [Competences]
      summary: Retirer compétence
      operationId: retirerCompetenceAventurier
      security:
        - bearerAuth: []
      responses:
        '204':
          description: Retirée
        '404':
          description: Ressource introuvable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '409':
          description: Suppression impossible
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompetenceProblemDetail'

  ########################
  # VUES
  ########################

  /api/v1/aventuriers/{id}/competences/disponibles:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags: [Competences]
      summary: Compétences disponibles/bloquées
      operationId: obtenirCompetencesDisponibles
      security:
        - bearerAuth: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompetencesDisponiblesResult'
        '404':
          description: Ressource introuvable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /api/v1/competences/{id}/aventuriers:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags: [Competences]
      summary: Aventuriers liés
      operationId: listerAventuriersParCompetence
      security:
        - bearerAuth: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AventuriersParCompetenceResponse'
        '404':
          description: Ressource introuvable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'


components:

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:

    AuthRequest:
      type: object
      properties:
        username:
          type: string
        password:
          type: string

    AuthResponse:
      type: object
      properties:
        token:
          type: string

    AventurierPayload:
      type: object
      required: [nom, physique, mental, perception, classe]
      properties:
        nom:
          type: string
          minLength: 3
        description:
            type: string
        physique:
            type: integer
            minimum: 1
            maximum: 50
        mental:
            type: integer
            minimum: 1
            maximum: 50
        perception:
            type: integer
            minimum: 1
            maximum: 50
        niveau:
            type: integer
            minimum: 1
            maximum: 100
        classe:
          type: string
          enum: [MAITRE_D_ARMES, ECLAIREUR, ARCANISTE, GARDIEN, PREDICATEUR, GUERRIER]

    AventurierDto:
      allOf:
        - type: object
          properties:
            id:
              type: string
              format: uuid
        - $ref: '#/components/schemas/AventurierPayload'

    Competence:
      type: object
      properties:
        id:
          type: string
          format: uuid
        nom:
          type: string
        description:
          type: string
        classeRequise:
          type: string
        niveauMinimum:
          type: integer
        physiqueMinimum:
          type: integer
        mentalMinimum:
          type: integer
        perceptionMinimum:
          type: integer
        competencesRequises:
          type: array
          items:
            type: string
            format: uuid

    AventuriersParCompetenceResponse:
      type: object
      properties:
        possesseurs:
          type: array
          items:
            $ref: '#/components/schemas/AventurierDto'
        eligibles:
          type: array
          items:
            $ref: '#/components/schemas/AventurierDto'
    
    CompetencesDisponiblesResult:
      type: object
      properties:
        disponibles:
          type: array
          items:
            $ref: '#/components/schemas/Competence'
        bloquees:
          type: array
          items:
            $ref: '#/components/schemas/Competence'

    ProblemDetail:
      type: object
      properties:
        type:
          type: string
        title:
          type: string
        status:
          type: integer
        detail:
          type: string

    AventurierProblemDetail:
      allOf:
        - $ref: '#/components/schemas/ProblemDetail'
        - type: object
          properties:
            prerequisManquants:
              type: array
              items:
                type: object

    CompetenceProblemDetail:
      allOf:
        - $ref: '#/components/schemas/ProblemDetail'
        - type: object
          properties:
            aventurierImpacté:
              type: array
              items:
                type: object

  responses:
    RequeteInvalide:
      description: Requête invalide
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'
    NonTrouve:
      description: Ressource introuvable
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'
    ErreurServeur:
      description: Erreur serveur
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'

    Unauthorized:
      description: Non authentifié
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'

    BadRequest:
      description: Requête invalide

    NotFound:
      description: Non trouvé

    Conflict:
      description: Conflit métier
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'

    UnprocessableEntity:
      description: Pré-requis non satisfaits
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'